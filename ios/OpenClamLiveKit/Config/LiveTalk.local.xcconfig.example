// Copy to LiveTalk.local.xcconfig. Never commit the real token.
// The public broker and LiveKit host are pinned in LiveTalk.xcconfig.
OPENCLAM_LIVETALK_APP_TOKEN = replace-with-the-internal-pilot-token
